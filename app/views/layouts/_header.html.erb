<header>

  <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-normal">MON CLUB THP</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <%= link_to "Accueil", root_path %>
        <a class="p-2 text-dark" href= '/post_login'>Espace club</a>

        <% if logged_in? %>
            <a href="#" class="dropdown dropdown-toggle" data-toggle="dropdown">
              Compte <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
        <% else %>
          <a class="btn btn-outline-primary" href="login" id="connexion">Se connecter</a>
          <a class="btn btn-outline-primary" href="signup">S'inscrire</a>

        <% end %>



      </nav>
    </div>
  </header>
